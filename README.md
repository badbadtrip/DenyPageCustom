# DenyPageCustom

Динамический плагин для [Lampac NextGen](https://github.com/lampac-nextgen/lampac), генерирующий кастомную страницу авторизации (`plugins/override/deny.js`) на основе секции `[DenyPage]` файла `init.conf`.

---

## Содержание

- [DenyPageCustom](#denypagecustom)
  - [Содержание](#содержание)
  - [Описание](#описание)
  - [Возможности](#возможности)
  - [Архитектура](#архитектура)
    - [Компоненты](#компоненты)
  - [Установка](#установка)
  - [Конфигурация](#конфигурация)
  - [Поля конфигурации](#поля-конфигурации)
    - [Форматы `tg_target`](#форматы-tg_target)
  - [Логика авторизации](#логика-авторизации)
    - [`checkAutch()`](#checkautch)
    - [`addDevice(message)`](#adddevicemessage)
  - [Адаптивная вёрстка](#адаптивная-вёрстка)
  - [Разработка](#разработка)
    - [Добавление нового поля конфигурации](#добавление-нового-поля-конфигурации)
    - [Структура сгенерированного файла](#структура-сгенерированного-файла)
    - [Безопасность](#безопасность)
  - [manifest.json](#manifestjson)

---

## Описание

При запуске сервера плагин читает параметры из `init.conf` и генерирует готовый JS-файл с полноэкранной страницей входа. Файл перезаписывается автоматически при каждом обновлении конфига — редактировать его вручную не нужно.

---

## Возможности

- **Полная кастомизация текстов** — заголовок, подзаголовок, шаги инструкции, подписи QR-блока
- **QR-код Telegram** — автоматически генерируется для любого Telegram-бота/канала; отображается только при наличии `tg_target` и `show_qr: true`
- **Горячая перезагрузка** — `"dynamic": true` позволяет применять изменения без перезапуска сервера
- **Хэш-диффинг** — файл записывается только при реальных изменениях (сравнение по хэшу содержимого)
- **Защита от XSS** — все строки из конфига экранируются через `JsonSerializer.Serialize`
- **Адаптивный дизайн** — корректно отображается на TV, планшетах и мобильных устройствах

---

## Архитектура

```
DenyPageCustom/
├── manifest.json          # Метаданные плагина для Lampac
├── ModInit.cs             # Точка входа: IModuleLoaded, таймер, подписка на события
├── DenyPageGenerator.cs   # Генератор JS-файла (чистый static builder)
└── Models/
    └── DenyPageConf.cs    # Модель конфигурации с дефолтами
```

### Компоненты

**`ModInit.cs`** — точка входа, реализует `IModuleLoaded`:
- При загрузке создаёт директорию вывода и вызывает `SyncAndGenerate()` один раз
- Подписывается на событие `EventListener.UpdateInitFile` (обновление `init.conf`)
- Запускает таймер с интервалом 3 секунды как резервный механизм синхронизации

**`DenyPageGenerator.cs`** — статический построитель:
- Метод `Build(DenyPageConf)` возвращает полное содержимое `deny.js` в виде строки
- Все JS-строки экранируются через `JsonSerializer.Serialize`
- Правая колонка с QR-кодом генерируется только если `tg_target` задан и `show_qr = true`
- Версия генератора указывается в комментариях (v3.0)

**`Models/DenyPageConf.cs`** — модель конфигурации:
- Заполняется через `ModuleInvoke.Init("DenyPage", new DenyPageConf())` из `init.conf`
- Все поля имеют значения по умолчанию; пустые строки получают дефолты внутри `Build()`

---

## Установка

Lampac NextGen использует **Roslyn** — `.cs` файлы компилируются сервером автоматически при старте. Предварительная сборка в DLL не требуется.

1. Скопируйте папку `DenyPageCustom` (со всеми `.cs` файлами и `manifest.json`) в директорию `mods/` рядом с исполняемым файлом Lampac:
   ```
   mods/
   └── DenyPageCustom/
       ├── manifest.json
       ├── ModInit.cs
       ├── DenyPageGenerator.cs
       └── Models/
           └── DenyPageConf.cs
   ```
2. Перезапустите сервер — Roslyn скомпилирует модуль автоматически. Скомпилированная сборка кэшируется; при последующих запусках без изменений исходников перекомпиляция не происходит.
3. При `"dynamic": true` (уже установлено в `manifest.json`) сервер следит за изменениями `.cs` файлов и перекомпилирует модуль **без перезапуска**.

Файл `plugins/override/deny.js` создаётся автоматически в директории установки сервера.

---

## Конфигурация

Все параметры задаются в секции `[DenyPage]` файла `init.conf`:

> Изменения применяются автоматически — перезапуск сервера не требуется.
```json
"DenyPage": {                            
    "tg_target":"@YourBot",
    "show_qr": true,
    "page_badge":"NextGen",                                 
    "page_title": "Вход в систему",
    "page_subtitle": "Для доступа к серверу введите пароль, выданный администратором.",
    "step1_text": "Введите пароль в поле ниже и нажмите «Войти».",
    "step2_text": "Если пароля нет — напишите боту <b>@YourBot</b>.",
    "qr_caption": "Нет пароля?",
    "qr_subcaption": "Отсканируй QR или нажми кнопку, чтобы написать боту.",
    "tg_button_text": "Написать боту"
  }
```
---

## Поля конфигурации

| Поле                   | Тип      | Умолчание (README)                              | Умолчание (код)                                                    | Статус                                                                                   |
| ---------------------- | -------- | ----------------------------------------------- | ------------------------------------------------------------------ | ---------------------------------------------------------------------------------------- |
| `tg_target`            | `string` | `""`                                            | `""`                                                               | Управляет QR-блоком. Принимает `@username`, `https://t.me/...` или `tg://` ссылку        |
| `show_qr`              | `bool`   | `true`                                          | `true`                                                             | QR-блок отображается только если `tg_target` задан **и** `show_qr = true`                |
| `page_title`           | `string` | `"Вход в систему"`                              | `"Вход в Lampa"`                                                   | Заголовок страницы — отрисовывается                                                      |
| `page_subtitle`        | `string` | `"Для доступа к серверу введите пароль..."`     | `"Доступ ограничен. Пароль можно получить у администратора."`      | Подзаголовок — отрисовывается                                                            |
| `step1_text`           | `string` | `"Введите пароль в поле ниже..."`               | `"Введите пароль в поле ниже и нажмите «Войти»."`                  | Текст шага 01 — отрисовывается                                                           |
| `step2_text`           | `string` | Авто (с упоминанием бота)                       | **Не используется в генераторе**                                   | Поле есть в модели, но не выводится в HTML                                               |
| `qr_caption`           | `string` | `"Нет пароля?"`                                 | `"Нет пароля?"`                                                    | Заголовок QR-блока — отрисовывается                                                      |
| `qr_subcaption`        | `string` | `"Отсканируй QR или нажми кнопку..."`           | `"Отсканируй QR или нажми кнопку, чтобы написать боту."`           | Подпись QR-блока — отрисовывается                                                        |
| `page_badge`           | `string` | `"Lampac NextGen"`                              | `"Lampac NextGen"`                                                 | Значение вычисляется, HTML-элемент удалён                                                |
| `hint_text`            | `string` | `""`                                            | `""`                                                               | В модели; не используется в генераторе                                                   |
| `footer_text`          | `string` | `""`                                            | `""`                                                               | В модели; не используется в генераторе                                                   |
| `tg_button_text`       | `string` | `"Открыть Telegram"`                            | `"Открыть Telegram"`                                               | Текст кнопки Telegram — используется из конфига                                          |
| `qr_size`              | `int`    | `200`                                           | `200` (в модели), `400` жёстко в коде                              | В модели есть, но в генераторе захардкожено `"400"`                                      |
| `show_password_button` | `bool`   | `true`                                          | `true`                                                             | В модели; не используется в генераторе (кнопка show/hide password есть всегда)           |
| `show_cub_button`      | `bool`   | `false`                                         | `false`                                                            | В модели; не используется в генераторе                                                   |
| `show_please_wait`     | `bool`   | `false`                                         | `false`                                                            | В модели; не используется в генераторе                                                   |
| `instruction_url`      | `string` | `""`                                            | `""`                                                               | В модели; не используется в генераторе                                                   |
| `instruction_text`     | `string` | `""`                                            | `""`                                                               | В модели; не используется в генераторе                                                   |

### Форматы `tg_target`

Все варианты нормализуются к корректной ссылке автоматически:

```
@mybotname          →  https://t.me/mybotname
mybotname           →  https://t.me/mybotname
https://t.me/bot    →  https://t.me/bot       (без изменений)
tg://resolve?...    →  tg://resolve?...        (без изменений)
```

---

## Логика авторизации

Сгенерированный `deny.js` внедряет две функции:

### `checkAutch()`
Вызывается немедленно при загрузке страницы. Отправляет GET-запрос на `{localhost}/testaccsdb` с текущими данными устройства (`account_email`, `lampac_unic_id`, `token`). Если ответ содержит `res.accsdb = true`:
- Скрывает UI приложения (`#app`)
- Устанавливает `window.sync_disable = true`
- Через 500 мс вызывает `addDevice()` (если `res.denymsg` не задан)
- Если `res.newuid` — генерирует и сохраняет новый `lampac_unic_id`

### `addDevice(message)`
Рендерит полноэкранную форму входа в `document.body`. Обрабатывает логин:

```
POST {localhost}/testaccsdb?account_email=<пароль>&uid=<uid>
```

| Результат                                      | Поведение                                                                             |
| ---------------------------------------------- | ------------------------------------------------------------------------------------- |
| `result.success = true`, `result.uid` задан    | Аккаунт создан — показывает UID, сохраняет в storage, редирект на `/` через 3 сек     |
| `result.success = true`, `result.uid` не задан | Сохраняет введённый пароль как `lampac_unic_id`, редирект на `/`                      |
| `result.success = false`                       | Показывает «Неправильный пароль», снимает блокировку кнопки                           |
| Ошибка сети                                    | Показывает «Ошибка соединения», снимает блокировку кнопки                             |

**Дополнительно:**
- Кнопка show/hide для пароля (глаз) — переключает `type="password"` ↔ `type="text"`
- Автофокус на поле ввода, удержание фокуса через интервал
- Поддержка Enter для отправки формы

> QR-изображение загружается в runtime с `api.qrserver.com` — не бандлится в плагин.

---

## Адаптивная вёрстка

| Разрешение / Условие              | Поведение                                                                                                       |
| --------------------------------- | --------------------------------------------------------------------------------------------------------------- |
| TV (≥ 1400px)                     | Макс. ширина 1100px, высота 85vh, увеличенные отступы, QR-код 180×180px для лучшей видимости на большом экране  |
| Desktop (> 900px)                 | Двухколоночный макет: форма слева, QR-блок справа (ширина 280px)                                                |
| Планшет (700–900px)               | Двухколоночный макет сжатый: ширина QR-блока 240px                                                              |
| Мобильный (≤ 700px)               | Одна колонка: QR-блок над формой (flex-direction: column), QR 120×120px                                         |
| Мобильный ландшафт (h ≤ 500px)    | Горизонтальная раскладка: QR-блок справа (border-left), ширина 220px                                            |
| Маленький мобильный (≤ 420px)     | Минимальные отступы (24px/20px) для экономии места                                                              |

---

## Разработка

### Добавление нового поля конфигурации

1. Добавьте свойство в `Models/DenyPageConf.cs` с дефолтным значением
2. Прочитайте значение в `DenyPageGenerator.Build()` и включите его в генерируемый HTML/JS
3. Обновите таблицу полей в этом README

### Структура сгенерированного файла

```
deny.js
├── CSS (инлайн, через <style>)
├── SVG-иконки как JS-переменные (warn, lock, eye, tg)
├── function addDevice(message)   — форма входа + обработчики
│   ├── Поле ввода пароля с иконкой замка
│   ├── Кнопка show/hide (глаз)
│   ├── Кнопка «Войти»
│   └── Обработчики: click, Enter, autofocus
└── function checkAutch()         — проверка авторизации + вызов addDevice
    └── checkAutch();             — немедленный вызов в конце файла
```

### Безопасность

- Все значения из конфига передаются в JS через `JsonSerializer.Serialize` — экранирование кавычек и спецсимволов гарантировано
- XSS-векторы минимизированы: единственное поле, поддерживающее HTML, не используется в текущей версии

---

## manifest.json

```json
{
  "name": "DenyPageCustom",
  "enable": true,
  "dynamic": true
}
```

| Поле        | Описание                                              |
| ----------- | ----------------------------------------------------- |
| `name`      | Идентификатор плагина (должен совпадать с именем DLL) |
| `enable`    | `true` — плагин активируется при старте               |
| `dynamic`   | `true` — горячая перезагрузка при изменении `.cs`     |

> Поле `version` отсутствует — не требуется для работы.
